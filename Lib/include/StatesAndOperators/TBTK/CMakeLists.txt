INSTALL(
	DIRECTORY .
	DESTINATION include/TBTK
	FILES_MATCHING PATTERN *.h
	PATTERN GaussianBasisGenerator.h EXCLUDE
)

IF(COMPILE_LIBINT2)
	INSTALL(
		DIRECTORY .
		DESTINATION include/TBTK
		FILES_MATCHING PATTERN GaussianBasisGenerator.h
	)
ENDIF(COMPILE_LIBINT2)
